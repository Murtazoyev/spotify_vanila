{
  "devDependencies": {
    "json-server": "^0.17.2"
  },
  "scripts": {
    "server": "json-server -w server/server.json -p 2442",
    "login": "json-server -w server/login.json -p 2552"
  },
  "dependencies": {
    "axios": "^1.3.4"
  }
}
